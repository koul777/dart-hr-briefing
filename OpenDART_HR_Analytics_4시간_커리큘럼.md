# OpenDART 기반 대화형 HR Analytics 에이전트

## 4시간 실습 커리큘럼

### 이 단계에서 만드는 것

GitHub 저장소에 보관되고 실제 웹 주소로 접속할 수 있는 나만의 OpenDART 기반 대화형 HR Analytics 에이전트를 완성한다.

완성된 저장소를 기반으로 다음 사용자 흐름을 구현하고 이해한다.

> 기업 선택 → OpenDART 조회 → HR 지표 계산 → 품질·개인정보 검증 → AI 브리핑 → GitHub·Vercel 배포

---

## 전체 시간표

| 시간 | 교육 모듈 | 완성 결과 |
|---|---|---|
| 13:00–13:50 | 모듈 1. 바이브 코딩 기본 활용법 — 이론 10분·실습 40분 | 수정 요청서와 첫 번째 코드 변경 |
| 13:50–14:00 | 휴식 |  |
| 14:00–14:50 | 모듈 2. OpenDART API 발급 및 대시보드 생성 — 이론 10분·실습 40분 | 비교기업·연도별 인력·보상·재무 대시보드 |
| 14:50–15:00 | 휴식 |  |
| 15:00–15:50 | 모듈 3. HR Analytics 대시보드 고도화하기 — 이론 10분·실습 40분 | 인력·보상·생산성 비교와 AI HR 브리핑 |
| 15:50–16:00 | 휴식 |  |
| 16:00–16:50 | 모듈 4. GitHub 저장 및 Vercel 배포 — 이론 10분·실습 40분 | GitHub 저장소와 공개 서비스 URL |
| 16:50–17:00 | 결과 발표·검증 | 최종 작동 확인 |

### 시간 구성 기준

- 핵심 수업: 200분
  - 이론: 40분(20%)
  - 실습: 160분(80%)
- 모듈 사이 휴식: 30분(10분 × 3회)
- 결과 공유·최종 검증: 10분
- 전체 과정: 240분(4시간)

---

## 모듈 1. 바이브 코딩 기본 활용법

**시간: 13:00~14:00(수업 50분 + 휴식 10분)**

### 이 단계에서 만드는 것

**기업과 연도를 선택할 수 있는 HR Analytics 대시보드의 기본 화면과 프로젝트 구조**

### 학습 목표

바이브 코딩의 개념을 이해하고, 만들고 싶은 프로그램의 요구사항을 Claude Code에 전달해 프로젝트를 시작한다. 기업과 기준연도를 선택할 수 있는 기본 화면을 단계적으로 만들고, 오류를 함께 수정하며 지시문을 구체화하는 반복 개발 방식을 익힌다.

### 1-1. 바이브 코딩의 개념과 활용 방식 — 이론 10분

바이브 코딩은 만들고 싶은 결과를 자연어로 설명하고, AI와 대화하면서 코드를 생성·수정·검증하는 개발 방식이다. AI가 모든 결정을 대신하는 방식이 아니라 사람이 목표와 기준을 정하고 AI가 탐색·구현·테스트를 지원하는 협업 방식으로 이해한다.

바이브 코딩에 적합한 활용:

- 아이디어를 작동하는 화면으로 빠르게 구체화
- 기존 프로젝트의 구조와 실행 흐름 파악
- 반복 작업과 기본 코드 생성
- 작은 기능 추가와 화면 문구·레이아웃 개선
- 오류 원인 탐색과 테스트 보조

사람이 반드시 확인할 사항:

- 요구사항과 우선순위
- API 키와 개인정보 등 보안 요소
- 계산식과 데이터 해석의 정확성
- AI가 변경한 파일과 코드 차이
- 실행·테스트 결과

기본 작업 순환:

```text
요구사항 설명
→ AI가 저장소와 관련 코드 확인
→ 변경 계획 합의
→ 작은 단위로 수정
→ 실행·테스트
→ 결과 확인과 추가 요청
```

### 1-2. AI가 이해할 수 있는 요구사항 작성 — 실습 8분

다음 양식으로 만들고 싶은 프로그램을 한 페이지 요구사항으로 작성한다.

```text
[프로그램 이름]
OpenDART 기반 대화형 HR Analytics 에이전트

[사용자]
상장기업의 인력·보상 구조를 비교하려는 HR 담당자

[해결할 문제]
여러 기업의 재무·직원·보상·임원 공시를 같은 기준으로 비교하기 어렵다.

[입력]
기업명, 기준연도, 보고서 종류, 사용자의 HR 질문

[핵심 화면과 기능]
1. 서비스 제목과 소개 영역
2. 기업 검색 입력창과 선택 기업 목록
3. 기준연도와 보고서 종류 선택
4. 비교 실행 버튼
5. 결과가 표시될 빈 대시보드 영역

[이번 단계에서 제외]
- 실제 OpenDART API 연결
- HR 지표 계산과 차트
- AI HR 브리핑
- Vercel 배포

[데이터와 기술]
OpenDART API, Python 서버, HTML/CSS/JavaScript, Vercel

[제약 조건]
- API 키를 코드와 GitHub에 저장하지 않는다.
- 데이터가 없으면 0으로 바꾸지 않는다.
- 공시 사실과 AI의 해석·가설을 구분한다.
- 개인 임원정보는 화면과 AI 응답에서 제외한다.

[완료 기준]
- 브라우저에서 HR Analytics 기본 화면이 열린다.
- 기업을 검색해 선택 목록에 추가·제거할 수 있다.
- 기준연도와 보고서 종류를 선택할 수 있다.
- 비교 버튼과 결과 표시 영역이 보인다.
- 화면·서버·배포 파일이 역할에 따라 구분되어 있다.
```

### 1-3. Claude Code를 활용한 프로젝트 생성 — 실습 7분

교육용 저장소를 개인 작업 폴더에 준비하고 Claude Code를 실행한다.

```powershell
git clone <교육용-GitHub-저장소-주소>
cd dart-hr-briefing
claude
```

Claude Code에서 먼저 프로젝트를 이해하도록 요청한다.

```text
이 저장소는 OpenDART 기반 HR Analytics 프로그램이야.
아직 코드를 수정하지 말고 README와 주요 파일을 확인한 뒤,
사용자 화면 → API → HR 지표 계산 → AI 브리핑 → 배포 흐름을 설명해줘.
```

필요한 경우 `/init`으로 프로젝트 지침 파일을 만들고 다음 원칙을 기록한다.

- 기존 기능을 유지하며 작은 단위로 수정
- 수정 전에 관련 파일과 계획 설명
- API 키와 개인정보를 코드에 포함하지 않음
- 수정 후 관련 검사와 테스트 실행

이번 단계에서 확인할 기본 프로젝트 구조:

```text
dart-hr-briefing/
├─ static/
│  ├─ index.html       # 대시보드 기본 화면
│  ├─ styles.css       # 화면 디자인
│  └─ app.js           # 기업·연도 선택 동작
├─ api/
│  └─ index.py         # Vercel Python 진입점
├─ server.py           # 로컬 서버와 API 경로
├─ .env.example        # API 키 항목 예시
├─ vercel.json         # 웹 배포 설정
└─ README.md           # 실행 방법과 프로젝트 설명
```

### 1-4. 화면과 기능을 단계별로 요청하는 방법 — 실습 10분

한 번에 전체 프로그램을 다시 만들어 달라고 요청하지 않고, 확인 가능한 단계로 나눈다.

```text
[1단계 요청]
HR Analytics 대시보드의 기본 화면을 만들어줘.

화면에는 다음 요소가 필요해.
- 서비스 제목과 한 줄 소개
- 기업 검색 입력창
- 선택한 기업을 보여주는 영역
- 기준연도 선택 목록
- 사업·반기·분기보고서 선택 목록
- “인력·보상 비교” 버튼
- 아직 결과가 없다는 안내 영역

기존 static/index.html과 static/styles.css를 사용하고,
아직 실제 OpenDART API와 AI 기능은 연결하지 마.
수정 파일과 변경 이유를 먼저 설명한 뒤 작업해줘.
```

1단계 결과를 확인한 다음 기능을 이어서 요청한다.

```text
[2단계 요청]
방금 만든 화면에 기본 선택 동작을 추가해줘.

- 샘플 기업을 선택 목록에 추가할 수 있게 해줘.
- 선택한 기업을 목록에서 제거할 수 있게 해줘.
- 최대 8개까지만 선택할 수 있게 해줘.
- 선택한 연도와 보고서 값을 화면 상태에 저장해줘.
- 비교 버튼을 누르면 현재 선택 조건을 결과 영역에 보여줘.

static/app.js에서 구현하고 아직 외부 API는 호출하지 마.
수정 후 JavaScript 문법 검사를 실행해줘.
```

단계별 요청 원칙:

- 한 요청에는 하나의 화면 또는 기능만 포함
- 유지해야 할 기존 동작 명시
- AI가 다음 단계까지 임의로 구현하지 않도록 범위 제한
- 각 단계마다 브라우저 또는 테스트로 확인 후 진행

### 1-5. 오류 메시지를 AI와 함께 확인하고 수정 — 실습 7분

실행이나 검사에서 오류가 발생하면 오류 문구를 생략하거나 바꾸지 않고 그대로 전달한다.

```text
node --check static/app.js 실행 중 아래 오류가 발생했어.

[오류 메시지 전체 붙여넣기]

1. 오류가 발생한 파일과 줄을 찾아줘.
2. 원인을 쉬운 말로 설명해줘.
3. 관련 없는 코드는 바꾸지 말고 최소 범위로 수정해줘.
4. 수정 후 같은 검사를 다시 실행해줘.
```

오류 수정 확인 순서:

1. 오류 메시지 전체 확인
2. AI가 설명한 원인과 실제 코드 비교
3. 최소 수정 적용
4. 동일한 명령으로 재검사
5. 화면의 기존 기능 회귀 확인

### 1-6. 지시문을 구체화하는 반복 개발 실습 — 실습 8분

첫 결과가 기대와 다르면 “다시 해줘”라고만 말하지 않고 차이를 구체적으로 설명한다.

```text
[현재 결과]
기업 선택 영역과 연도 선택 영역의 간격이 너무 좁고
모바일 화면에서 비교 버튼이 옆으로 잘린다.

[원하는 결과]
두 선택 영역을 명확히 구분하고 모바일에서는 세로로 배치해줘.
비교 버튼은 모바일 화면 너비에 맞게 보여줘.

[유지할 사항]
현재 제목, 색상, 기업 추가·제거 기능과 연도 선택 동작은 변경하지 마.

[확인 방법]
수정한 문구와 파일 위치를 알려주고 기존 문법 검사도 다시 실행해줘.
```

반복 개발 순환:

```text
초기 지시
→ 결과 확인
→ 기대와 다른 점 기록
→ 제약·예시·완료 기준 추가
→ 재수정
→ 실행·테스트
→ 완료 판단
```

### 모듈 1 완료 산출물

- AI가 이해할 수 있는 프로그램 요구사항 1부
- Claude Code로 초기화한 개인 프로젝트
- 기업·연도 선택이 가능한 HR Analytics 기본 화면
- `static`, `api`, 서버·배포 설정이 구분된 프로젝트 구조
- 화면 요청과 선택 기능 요청 각 1개
- 오류 메시지를 이용한 수정 경험 1회
- 지시문을 구체화해 결과를 개선한 전후 기록

**13:50~14:00 휴식**

---

## 모듈 2. OpenDART API 발급 및 대시보드 생성

**시간: 14:00~15:00(수업 50분 + 휴식 10분)**

### 이 단계에서 만드는 것

**비교기업과 연도를 선택하면 OpenDART에서 인력·보상·재무 데이터를 불러와 보여주는 기본 HR Analytics 대시보드**

### 학습 목표

OpenDART 인증키를 발급받아 안전하게 프로젝트에 연결하고, 모듈 1에서 만든 기업·연도 선택 화면에 실제 기업 검색과 공시 데이터 조회 기능을 연결한다.

### 2-1. OpenDART API와 데이터 구조 이해 — 이론 10분

OpenDART는 금융감독원 전자공시 자료를 프로그램에서 조회할 수 있도록 제공하는 Open API다. 이번 모듈에서는 기업 검색, 직원 현황, 재무정보 세 영역에 집중한다.

| OpenDART 데이터 | API·파일 | 대시보드 활용 |
|---|---|---|
| 기업 고유번호 | `corpCode.xml` | 기업명·종목코드 검색 |
| 단일회사 재무계정 | `fnlttSinglAcnt.json` | 매출·영업이익·재무구조 |
| 직원 현황 | `empSttus.json` | 직원 수·고용형태·근속·급여 |

주요 요청값:

- `crtfc_key`: 발급받은 API 인증키
- `corp_code`: DART가 사용하는 8자리 기업 고유번호
- `bsns_year`: 사업연도
- `reprt_code`: 사업·반기·분기보고서 구분

같은 기업이라도 연도와 보고서 종류가 다르면 비교 기준이 달라질 수 있으므로, 선택한 기업들에 동일한 연도와 보고서 코드를 적용한다.

### 2-2. OpenDART API 인증키 발급과 환경변수 설정 — 실습 8분

1. [OpenDART 공식 사이트](https://opendart.fss.or.kr/)에 접속한다.
2. 회원가입 또는 로그인 상태를 확인한다.
3. **인증키 신청** 메뉴에서 현재 안내에 따라 인증키를 신청한다.
4. `.env.example`을 `.env`로 복사한다.
5. 발급된 키를 `.env`의 `OPENDART_API_KEY`에 입력한다.
6. 서버 실행 후 `/api/health`에서 연결 상태를 확인한다.

```powershell
Copy-Item .env.example .env
# .env에 OPENDART_API_KEY 입력
python server.py
```

보안 원칙:

- 인증키를 Python·JavaScript 파일에 직접 작성하지 않는다.
- 인증키가 포함된 URL을 화면 캡처하거나 공유하지 않는다.
- `.env`를 GitHub에 커밋하지 않는다.
- `/api/health` 응답에는 연결 여부만 표시하고 키 값은 반환하지 않는다.

### 2-3. 기업명으로 DART 고유번호 조회 — 실습 6분

OpenDART의 데이터 조회에는 종목코드가 아니라 8자리 `corp_code`가 필요하다. 현재 프로그램은 `corpCode.xml`을 내려받아 기업명·종목코드·고유번호 검색 목록을 만든다.

사용자 흐름:

```text
기업명 입력
→ /api/companies?q=삼성전자
→ server.py의 search_companies()
→ corpCode.xml 기업 목록 검색
→ corp_name·stock_code·corp_code 반환
```

Claude Code 요청 예시:

```text
기업 검색창을 /api/companies에 연결해줘.
사용자가 기업명을 두 글자 이상 입력하면 검색하고,
검색 결과에는 기업명과 종목코드를 보여줘.
기업을 선택하면 corp_code를 화면 상태에 저장해줘.
검색 중·결과 없음·오류 상태를 구분해줘.
```

### 2-4. 직원현황과 재무정보 API 연결 — 실습 8분

선택한 기업의 `corp_code`, 사업연도, 보고서 코드를 두 API에 동일하게 전달한다.

```text
/api/financials
→ OpenDART fnlttSinglAcnt.json
→ 매출액·영업이익 정규화

/api/people
→ OpenDART empSttus.json
→ 직원 수·평균급여·평균 근속 정규화
```

Claude Code 요청 예시:

```text
선택한 기업들의 corp_code와 선택 연도·보고서 코드를 사용해서
/api/financials와 /api/people을 함께 호출해줘.

요청값은 corp_codes, year, report_code 형식으로 맞추고
두 API의 결과를 corp_code 기준으로 결합해줘.

제약 조건:
- OPENDART_API_KEY를 브라우저로 보내지 않는다.
- 기존 서버 API 경로를 변경하지 않는다.
- 로딩·데이터 없음·오류 상태를 구분한다.
```

### 2-5. 핵심 HR·재무 데이터 불러오기 — 실습 8분

OpenDART 원자료를 그대로 화면에 출력하지 않고 현재 프로그램의 정규화 로직을 사용해 다섯 가지 지표를 만든다.

| 대시보드 지표 | OpenDART 원자료·계산 방식 | 프로그램 지표명 |
|---|---|---|
| 직원 수 | 직원 현황의 `sm` 합계 | `employees_total` |
| 1인 평균급여액 | 연간 급여총액 ÷ 직원 수, 없으면 `jan_salary_am` 가중 평균 | `average_salary` |
| 평균 근속연수 | `avrg_cnwk_sdytrn`을 직원 수로 가중 평균 | `average_tenure_years` |
| 매출액 | 재무계정의 매출액·수익 계정명 정규화 | `revenue` |
| 영업이익 | 재무계정의 영업이익·영업이익(손실) 정규화 | `operating_profit` |

확인할 원칙:

- 성별 합계 행과 세부 행을 중복 집계하지 않는다.
- 쉼표·공백·단위가 포함된 숫자를 정규화한다.
- 공시되지 않은 값은 0이 아니라 `null` 또는 데이터 없음으로 유지한다.
- 여러 기업의 평균을 낼 때는 필요한 경우 직원 수 가중 평균을 사용한다.

### 2-6. 비교기업과 연도를 선택하는 기능 구현 — 실습 5분

모듈 1의 기본 선택 화면을 실제 데이터 요청 상태와 연결한다.

- 기업명 검색 결과에서 최대 8개 기업 선택
- 선택 기업 추가·삭제
- 기준연도 선택
- 사업·반기·분기보고서 선택
- 선택 조건이 바뀌면 기존 결과의 재조회 필요 안내
- 비교 버튼을 눌렀을 때 선택 기업 전체를 동일 기준으로 조회

### 2-7. 표와 핵심 지표로 기본 대시보드 생성 — 실습 5분

1. 삼성전자 또는 다른 상장기업을 검색한다.
2. 비교기업을 한 곳 이상 추가한다.
3. 기준연도와 보고서 종류를 선택한다.
4. **인력·보상 비교**를 실행한다.
5. 핵심 KPI 카드에 직원 수·평균급여·평균 근속을 표시한다.
6. 비교표에 기업별 직원 수·평균급여·평균 근속·매출·영업이익을 표시한다.
7. 값이 없는 셀에는 `데이터 없음`을 표시한다.

검증 항목:

- 기업명 검색 결과가 실제 OpenDART 기업과 일치하는가?
- 선택한 연도와 보고서 코드가 API 요청에 반영되는가?
- 다섯 가지 지표의 단위와 숫자 형식이 올바른가?
- 로딩 중·데이터 없음·API 오류가 서로 다르게 표시되는가?
- 인증키가 HTML, JavaScript, 주소창, API 응답에 노출되지 않는가?

### 모듈 2 완료 산출물

- 개인 OpenDART API 인증키 발급 및 `.env` 연결
- 기업명으로 DART 고유번호를 찾는 검색 기능
- 직원현황과 재무정보 API 연결
- 직원 수·1인 평균급여액·평균 근속연수·매출액·영업이익 조회
- 비교기업·기준연도·보고서 선택 기능
- 핵심 KPI 카드와 기업 비교표로 구성된 기본 대시보드
- API 키 미노출과 데이터 없음 처리 확인 기록

**14:50~15:00 휴식**

---

## 모듈 3. HR Analytics 대시보드 고도화하기

**시간: 15:00~16:00(수업 50분 + 휴식 10분)**

### 이 단계에서 만드는 것

**기업의 인력·보상·생산성 지표를 비교하고, 분석 결과를 HR 관점에서 설명하는 대화형 AI HR 브리핑 대시보드**

### 학습 목표

모듈 2에서 연결한 OpenDART 인력·재무 데이터를 결합해 생산성 지표와 비교 시각화를 만든다. 검증된 지표만 생성형 AI에 전달하고, 확인된 사실·HR 해석·가설·추가 내부 데이터·KPI를 구분하는 대화형 HR 브리핑을 구현한다.

### 3-1. 데이터 결합과 AI 해석의 기본 원칙 — 이론 10분

HR Analytics 대시보드는 공시 숫자, 계산한 지표, AI 해석을 서로 다른 층으로 구분한다.

| 구분 | 예시 | 처리 원칙 |
|---|---|---|
| 확인된 사실 | 직원 수 12,000명, 평균급여 8,500만원 | 기업명·연도·단위·출처 표시 |
| 계산 지표 | 인당 매출, 인당 영업이익 | 계산식과 분모 확인 |
| HR 관점 해석 | 높은 인당 매출이 생산성 신호일 가능성 | 사실과 분리해 해석으로 표시 |
| 가설 | 자동화 또는 인력구성 변화의 영향 가능성 | 원인으로 단정하지 않음 |
| 추가 검증 | 이직·채용·직급·직무·성과 데이터 | 내부 데이터 필요 표시 |

핵심 원칙:

- 같은 `corp_code`·사업연도·보고서 코드로 인력정보와 재무정보를 결합한다.
- 금액 단위와 직원 수 단위를 계산 전에 통일한다.
- 값이 없거나 분모가 0이면 지표를 계산하지 않는다.
- 공시에서 확인되지 않은 원인을 AI가 만들어내지 않게 한다.
- AI는 계산기가 아니라 검증된 지표의 해석과 후속 질문을 지원한다.

### 3-2. 인력정보와 재무정보 연결 및 생산성 지표 계산 — 실습 7분

`corp_code`, 연도, 보고서 코드를 공통 키로 `/api/people`과 `/api/financials` 결과를 결합한다.

```text
인당 매출액 = 매출액 ÷ 직원 수
인당 영업이익 = 영업이익 ÷ 직원 수
급여/매출 비율 = 연간 급여총액 ÷ 매출액 × 100
정규직 비중 = 정규직 수 ÷ 전체 직원 수 × 100
```

Claude Code 요청 예시:

```text
기업별 people 결과와 financials 결과를 corp_code 기준으로 결합해줘.
인당 매출액과 인당 영업이익을 계산하되 직원 수가 없거나 0이면
계산하지 말고 null로 유지해줘. 계산식과 사용 단위도 화면에 표시해줘.
```

### 3-3. 기업별·연도별 직원·급여·근속 변화 비교 — 실습 6분

`/api/people/history`와 `/api/financials/history`를 사용해 다음 변화를 동일한 연도 축으로 비교한다.

- 직원 수
- 1인 평균급여액
- 평균 근속연수
- 매출액
- 영업이익
- 인당 매출액과 인당 영업이익

첫 연도와 최근 연도의 값, 증감률, 데이터가 존재하는 실제 연도 범위를 함께 표시한다.

### 3-4. 비교표·시각화와 데이터 단위·결측값 처리 — 실습 7분

화면 구성:

- 기업별 핵심 KPI 카드
- 기업·연도별 비교표
- 직원 수·급여·근속 추이 선 그래프
- 매출·영업이익·인당 지표 비교 그래프

표시 규칙:

| 데이터 | 표시 단위 예시 | 결측값 처리 |
|---|---|---|
| 직원·임원 수 | 명 | `데이터 없음` |
| 평균 근속 | 년, 소수점 1자리 | 선을 0까지 연결하지 않음 |
| 급여 | 만원 또는 억원 | 원자료 단위 확인 후 변환 |
| 매출·영업이익 | 억원 | 음수 영업이익 부호 유지 |
| 비율 | %, 소수점 1자리 | 분모가 없으면 계산하지 않음 |

공시 누락은 0과 다르며, API 오류도 단순 결측값과 구분해 표시한다.

### 3-5. 생성형 AI HR 브리핑과 내부 데이터·KPI 제안 — 실습 14분

대시보드에서 선택한 기업·연도와 정규화된 지표를 `orchestrator.py`의 분석 컨텍스트로 조립하고 `openai_responses_adapter.py`를 통해 AI에 전달한다.

```text
사용자 질문
→ 선택 기업·연도 확인
→ 인력·재무·생산성 지표 조립
→ 누락·오류·출처 포함
→ OpenAI Responses API
→ 한국어 HR 브리핑
```

사용자가 입력한 OpenAI API 키는 저장소나 `localStorage`에 저장하지 않고 해당 요청의 헤더로만 전달한다.

추천 실습 질문:

> 직원 수·평균급여·인당 영업이익의 변화를 기업별로 비교하고, 확인된 사실과 가능한 HR 가설을 구분해줘.

`HR_BRIEFING_RULES.md`의 출력 구조:

```text
1. 핵심 요약
2. 확인된 공시 사실
3. HR 관점의 해석
4. 추가 검증이 필요한 가설
5. 추가로 확인할 내부 데이터
6. 추적할 KPI
7. 데이터 한계와 주의사항
```

추가 내부 데이터 예시:

- 입·퇴사와 자발적 이직 데이터
- 채용 소요기간과 오퍼 수락률
- 직급·직무·지역별 인원과 보상
- 성과등급과 승진·이동 이력
- 근로시간·교육·몰입도 데이터

추가 KPI 예시:

- 자발적 이직률과 핵심인재 유지율
- 채용 소요기간과 오퍼 수락률
- 인당 매출·인당 영업이익
- 인건비/매출 비율
- 직급·직무별 보상 수준과 격차
- 내부 이동률과 승진률

위 내부 데이터와 KPI는 OpenDART에서 확인된 사실이 아니라 후속 검증 항목임을 명시한다.

### 3-6. 후속 질문이 가능한 대화형 기능 추가 — 실습 6분

`static/app.js`의 대화 상태에 최근 질문과 답변을 유지하고 새 질문에 현재 기업·연도 컨텍스트를 다시 결합한다.

```text
첫 질문: 인당 영업이익이 가장 높은 기업을 설명해줘.
후속 질문: 그 기업의 평균급여 변화도 함께 비교해줘.
추가 질문: 이 가설을 검증할 내부 KPI 세 개만 추천해줘.
```

대화형 기능 검증:

- 후속 질문에도 같은 기업·연도·보고서 기준이 유지되는가?
- AI가 이전 답변을 사실처럼 재사용하지 않고 DART 컨텍스트를 다시 확인하는가?
- 기업이나 연도를 변경하면 새로운 분석 컨텍스트가 적용되는가?
- API 키가 대화 기록·서버 로그·브라우저 저장소에 남지 않는가?
- AI 오류가 성공한 브리핑처럼 표시되지 않는가?

### 모듈 3 완료 산출물

- 인력·재무 데이터가 결합된 기업별 분석 레코드
- 인당 매출액·인당 영업이익 등 생산성 지표
- 기업별·연도별 직원 수·급여·근속·재무 변화 비교
- 단위와 결측값이 처리된 비교표·추이 시각화
- 확인된 사실과 HR 해석·가설이 구분된 AI 브리핑
- 추가 내부 데이터와 KPI 제안
- 동일한 DART 근거로 이어가는 후속 질문 기능

**15:50~16:00 휴식**

---

## 모듈 4. GitHub 저장 및 Vercel 배포

**시간: 16:00~17:00(수업 50분 + 최종 검증 10분)**

### 이 단계에서 만드는 것

**완성한 대화형 AI HR 브리핑 대시보드를 GitHub 저장소에 보관하고, 누구나 웹 주소로 접속할 수 있도록 Vercel에 배포한 서비스**

### 학습 목표

완성한 프로그램에서 비밀키와 불필요한 파일을 제외한 뒤 GitHub 저장소에 커밋·푸시한다. GitHub 저장소를 Vercel 프로젝트에 연결하고 서버 환경변수를 등록해 실제 웹 주소로 배포하고 검증한다.

### 4-1. GitHub·Vercel 배포 흐름과 보안 경계 — 이론 10분

다음 저장·배포·업데이트 흐름을 이해한다.

```text
로컬 프로젝트
→ Git 변경사항 저장
→ GitHub 원격 저장소
→ Vercel Git 연결
→ Python Function(api/index.py)
→ OpenDART·OpenAI API
→ 공개 웹 주소
```

핵심 원칙:

- GitHub에는 소스코드와 빈 설정 예시인 `.env.example`만 저장한다.
- 실제 OpenDART 인증키는 로컬 `.env`와 Vercel 환경변수에 각각 보관한다.
- 일반 사용자의 OpenAI 키는 브라우저 탭에서 요청 헤더로만 전달한다.
- `main` 브랜치에 새 변경사항을 푸시하면 연결된 Vercel 배포가 갱신된다.
- 환경변수를 변경하면 기존 배포가 아니라 새 배포에 적용되는지 확인한다.

### 4-2. 프로젝트 파일·폴더 구조와 API 키 분리 최종 점검 — 실습 7분

배포에 필요한 구조를 확인한다.

```text
static/                 # 대시보드 화면·스타일·동작
api/index.py            # Vercel Python 진입점
server.py               # 로컬 서버와 API 요청
workforce_analytics.py  # HR 지표 계산
orchestrator.py         # AI 분석 컨텍스트
HR_BRIEFING_RULES.md    # HR 브리핑 규칙
vercel.json             # 배포 경로 설정
.env.example            # 환경변수 이름만 제공
README.md               # 실행·배포·사용 방법
```

비밀키 점검:

```powershell
git status
git check-ignore .env
git ls-files .env
```

- `.env`는 `.gitignore`에 포함한다.
- `.env.example`에는 변수 이름만 남기고 실제 값은 비워둔다.
- `git ls-files .env` 결과가 없어야 한다.
- 이미 공개 저장소에 키가 올라갔다면 파일 삭제만으로 끝내지 않고 해당 키를 폐기·재발급한다.

### 4-3. GitHub 저장소 생성과 프로젝트 업로드 — 실습 8분

1. GitHub에서 새 저장소를 생성하거나 교육용 저장소를 자신의 계정으로 Fork한다.
2. 저장소 이름과 설명을 입력하고 공개·비공개 범위를 확인한다.
3. 로컬 프로젝트의 원격 저장소가 자신의 저장소인지 확인한다.
4. 변경 파일을 검토한 뒤 커밋하고 `main` 브랜치에 푸시한다.

```powershell
git remote -v
git status
git add -A
git status
git diff --cached --name-only
git commit -m "Build OpenDART AI HR briefing dashboard"
git push origin main
```

GitHub 웹 화면에서 프로젝트 파일, 최근 커밋, README가 정상적으로 표시되는지 확인한다.

### 4-4. Vercel·GitHub 연결과 환경변수 등록 — 실습 8분

1. Vercel에 로그인하고 **New Project**를 선택한다.
2. 방금 만든 GitHub 저장소를 Import한다.
3. 프로젝트 루트와 `vercel.json`, `api/index.py`를 확인한다.
4. Production 환경에 `OPENDART_API_KEY`를 등록한다.
5. 사용할 모델 이름을 `OPENAI_MODEL`에 등록한다.
6. 사용자가 자신의 AI 키를 입력하는 구조라면 서버의 `OPENAI_API_KEY`는 생략한다.
7. Preview 환경도 사용할 경우 필요한 환경변수를 별도로 등록한다.

API 키는 소스코드, `vercel.json`, 저장소 설명, 배포 URL에 적지 않는다.

### 4-5. 웹 서비스 배포와 실제 접속 주소 검증 — 실습 7분

Deploy를 실행한 뒤 생성된 `vercel.app` 주소에서 다음 순서로 확인한다.

1. 첫 화면과 정적 파일이 정상적으로 열린다.
2. `/api/health`에서 OpenDART 연결 상태를 확인한다.
3. 기업명 검색과 비교기업 선택을 실행한다.
4. 기준연도와 보고서를 선택해 데이터를 조회한다.
5. 직원·급여·근속·매출·영업이익과 생산성 지표를 확인한다.
6. AI HR 브리핑과 후속 질문을 실행한다.
7. 브라우저 개발자 도구에서 인증키가 노출되지 않는지 확인한다.

### 4-6. 오류 수정 후 GitHub 저장과 자동 재배포 — 실습 5분

배포 오류가 발생하면 다음 순서로 처리한다.

```text
Vercel 배포 로그 확인
→ 오류 메시지 전체를 Claude Code에 전달
→ 로컬에서 같은 오류 재현
→ 최소 범위로 수정
→ 테스트·문법 검사
→ Git 커밋과 push
→ 새 Vercel 배포 확인
```

확인 명령:

```powershell
python -m unittest discover -v
python -m py_compile server.py workforce_analytics.py orchestrator.py agent_orchestration.py
node --check static/app.js
```

환경변수를 추가·수정한 경우에도 새 배포 또는 재배포 후 적용 여부를 확인한다.

### 4-7. 다음 기업 분석에 재사용할 수 있도록 프로젝트 정리 — 실습 5분

- README에 서비스 URL, 기능, 실행 방법, 환경변수 이름을 정리한다.
- 분석 대상 기업을 코드에 고정하지 않고 검색·선택 방식으로 유지한다.
- 기준연도와 보고서 종류를 화면에서 변경할 수 있게 유지한다.
- HR 브리핑 규칙을 `HR_BRIEFING_RULES.md`에서 별도로 수정할 수 있게 한다.
- 데이터 출처, 계산식, 단위, 결측값 처리 원칙을 문서화한다.
- 알려진 제한사항과 추가 내부 데이터 필요 영역을 기록한다.
- 다음 변경사항도 `수정 → 테스트 → 커밋 → push → 자동 재배포` 순서로 관리한다.

### 모듈 4 완료 산출물

- API 인증키가 제외된 정리된 프로젝트 구조
- 개인 GitHub 저장소와 최종 커밋
- GitHub와 연결된 Vercel 프로젝트
- 안전하게 등록된 서버 환경변수
- 실제 접속 가능한 웹 서비스 주소
- 기업 검색·데이터 비교·AI 브리핑 작동 확인 기록
- 오류 수정과 재배포 경험
- 다음 기업 분석에도 재사용할 수 있는 README와 설정 구조

**16:50~17:00 결과 공유·최종 작동 검증**

---

## 최종 산출물

수강생 한 명당 다음 결과가 남아야 한다.

1. 자신의 GitHub 저장소
2. 실제 접속 가능한 Vercel URL
3. OpenDART 기반 기업 비교 화면
4. 인력·보상·생산성 비교표와 추이 시각화
5. 조직 목적에 맞게 수정한 HR 브리핑 규칙
6. 대화형 AI 질문과 후속 질문 기능
7. API 키가 제외된 안전한 배포 구조

---

## 수업 운영 원칙

4시간 안에 백엔드와 프론트엔드를 처음부터 모두 개발하지 않는다. 제공된 완성형 프로그램을 기반으로 다음 네 가지 결과를 완주한다.

> 기본 화면 생성 → OpenDART 연결 → 분석·AI 브리핑 고도화 → GitHub·Vercel 공개 배포

사전 준비가 필요한 항목은 수업 전에 완료한다.

- GitHub 계정
- Vercel 계정 및 GitHub 연결
- OpenDART API 인증키
- OpenAI API 키
- Python 3.11 이상
- Git 및 최신 웹 브라우저
