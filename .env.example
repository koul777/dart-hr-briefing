# OpenDART API 인증키(40자리)
OPENDART_API_KEY=

# Optional: only when a separately provisioned Claude Code MCP gateway exists.
CLAUDE_MCP_GATEWAY_URL=
CLAUDE_MCP_GATEWAY_TOKEN=
CLAUDE_MCP_GATEWAY_TIMEOUT_SECONDS=20
