# 발표자 노트

이 덱은 완성 프로그램 소개 자료가 아니라 실습 진행 화면입니다. 각 슬라이드에서 강사는 먼저 직접 입력하고, 수강생이 같은 조작을 끝낸 뒤 다음 장으로 이동합니다.

## 01. DART API부터 배포까지 직접 만들어가기
강의 목표와 제출 증거를 짧게 확인합니다.

## 02. 빈 Starter를 열고, 프롬프트로 기능을 한 단계씩 붙인다
강의 목표와 제출 증거를 짧게 확인합니다.

## 03. 완성 화면은 한 번만 보고 바로 닫는다
강의 목표와 제출 증거를 짧게 확인합니다.

## 04. 설명보다 입력·수정·검증 시간이 더 길다
강의 목표와 제출 증거를 짧게 확인합니다.

## 05. 완성 화면보다 이 네 가지 기록을 제출한다
강의 목표와 제출 증거를 짧게 확인합니다.

## 06. OpenDART 키와 개발 환경 준비
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 07. 네 계정과 편집·실행 도구를 먼저 확인한다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 08. Starter를 복사하고 기준 커밋을 만든다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 09. Claude Code를 실행하고 프로젝트 폴더 안인지 확인한다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 10. OpenDART에서 인증키 신청 메뉴를 연다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 11. 약관 동의와 필수 항목을 입력하고 등록한다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 12. 인증키 관리에서 키를 복사하되 화면에는 남기지 않는다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 13. .env.example을 복사해 로컬 전용 .env를 만든다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 14. 인증키는 .env에만 넣고 저장한다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 15. 서버를 켜기 전에 .env가 Git에서 제외되는지 확인한다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 16. Python 서버를 실행하고 실제 로컬 주소를 연다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 17. 키 값 대신 “설정됨” 상태만 확인한다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 18. 전체 프롬프트를 붙여넣고 Enter를 누른다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 19. 첫 요청은 코드를 만들라는 말이 아니라 “읽어달라”는 요청이다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 20. 읽는 파일과 실행하려는 명령을 화면에서 확인한다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 21. 답변에서 다섯 파일의 역할을 찾아 표시한다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 22. 목적·입력·규칙·완료 기준을 한 번에 준다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.

## 23. Claude Code로 기본 화면 만들기
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 24. 구현 전에 변경 파일과 완료 기준부터 받는다
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 25. 계획이 이번 범위를 넘으면 구현 전에 줄인다
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 26. 서비스 제목과 입력 영역만 만드는 프롬프트를 넣는다
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 27. 파일 경로와 수정 내용을 보고 승인한다
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 28. 파일이 만들어진 뒤 terminal에서 diff를 본다
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 29. 같은 대화에서 기업 선택 동작만 추가한다
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 30. 프롬프트 한 번으로 화면 상태가 어떻게 바뀌었는지 비교한다
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 31. 기업 선택은 유지하고 기간과 비교 버튼만 붙인다
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 32. 서버를 다시 열고 추가된 기능을 손으로 조작한다
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 33. 오류 화면을 그대로 보여주고 작은 수정만 요청한다
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 34. 프롬프트 세 번으로 화면이 자라는 과정을 설명해야 통과한다
프롬프트를 붙여넣고 계획·변경 파일·diff·브라우저 결과를 차례로 확인합니다.

## 35. OpenDART 연결과 HR 지표·AI 구현
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 36. 브라우저에 키를 보내지 않고 서버가 OpenDART를 호출한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 37. 세 API와 요청값의 차이를 먼저 고정한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 38. 실제 코드를 쓰기 전에 호출·결측·검증 계획을 받는다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 39. .env를 읽는 위치와 health 응답이 안전한지 Claude로 검사한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 40. 화면 검색어를 DART corp_code로 바꾸는 경로를 붙인다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 41. 검색어를 우리 서버의 /api/companies로 보낸다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 42. Python 서버가 검색어를 받고 기업 목록을 반환한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 43. 키를 붙여 받은 ZIP을 서버에서 XML로 푼다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 44. 브라우저 주소창에서 우리 API의 JSON을 먼저 확인한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 45. 검색 결과에서 회사명·종목코드·corp_code를 확인한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 46. 두 API 결과를 같은 corp_code와 기간으로 결합한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 47. 선택한 corp_code와 기간을 우리 서버 경로로 보낸다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 48. 같은 기간 파라미터를 두 OpenDART API에 전달한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 49. corp_code를 열쇠로 두 응답을 한 기업 행으로 합친다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 50. 값이 없을 때 0으로 보이면 분석이 틀어진다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 51. 비교 결과에서 값·단위·근거를 직접 확인한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 52. 공시 원자료를 계산 함수로 바꾸는 프롬프트를 넣는다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 53. 정상값 하나보다 결측·0·음수 사례를 함께 본다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 54. AI에게 문서 전체가 아니라 검증된 숫자만 보낸다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 55. 질문과 숫자, 계산, 근거 ID가 한 묶음이 된다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 56. Provider·경로·키 수명까지 프롬프트에 적는다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 57. 검증된 payload와 선택적 개인 키를 우리 서버로 보낸다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 58. 서버가 Responses API를 호출해 답변만 반환한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 59. 질문을 넣고 답변보다 근거 구조를 검사한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 60. 프롬프트에서 실제 공시와 근거 있는 답변까지 이어져야 통과한다
OpenDART 연결, 결측, 계산, AI Provider 경로를 한 기능씩 구현하고 매번 실행합니다.

## 61. GitHub에 저장하고 Vercel에 배포하기
GitHub와 Vercel에서 실제 URL이 생길 때까지 진행하고 원격 commit을 대조합니다.

## 62. git add 전에 .env와 키가 빠졌는지 다시 확인한다
GitHub와 Vercel에서 실제 URL이 생길 때까지 진행하고 원격 commit을 대조합니다.

## 63. github.com/new에서 빈 저장소를 만든다
GitHub와 Vercel에서 실제 URL이 생길 때까지 진행하고 원격 commit을 대조합니다.

## 64. 기준 커밋 뒤의 변경만 검토해 원격 저장소로 보낸다
GitHub와 Vercel에서 실제 URL이 생길 때까지 진행하고 원격 commit을 대조합니다.

## 65. 저장소 웹 화면에서 코드와 비밀 분리를 확인한다
GitHub와 Vercel에서 실제 URL이 생길 때까지 진행하고 원격 commit을 대조합니다.

## 66. vercel.com/new에서 방금 Push한 저장소를 가져온다
GitHub와 Vercel에서 실제 URL이 생길 때까지 진행하고 원격 commit을 대조합니다.

## 67. Production 환경에 OpenDART 키를 다시 등록한다
GitHub와 Vercel에서 실제 URL이 생길 때까지 진행하고 원격 commit을 대조합니다.

## 68. Deploy를 누르고 Ready보다 Build Logs를 먼저 읽는다
GitHub와 Vercel에서 실제 URL이 생길 때까지 진행하고 원격 commit을 대조합니다.

## 69. 배포 URL에서 로컬과 같은 순서로 다시 조작한다
GitHub와 Vercel에서 실제 URL이 생길 때까지 진행하고 원격 commit을 대조합니다.

## 70. 코드를 고치면 같은 검증 루프를 한 번 더 돈다
GitHub와 Vercel에서 실제 URL이 생길 때까지 진행하고 원격 commit을 대조합니다.

## 71. 저장소와 URL이 같은 코드를 보여줘야 통과한다
GitHub와 Vercel에서 실제 URL이 생길 때까지 진행하고 원격 commit을 대조합니다.

## 72. 키를 넣고 프롬프트로 만들고 검증해서 배포했다
수강생 화면에서 계정·키·.env·서버·Claude 입력을 직접 따라 하게 합니다.
